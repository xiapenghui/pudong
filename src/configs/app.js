export default {
  /**
   * 当前应用版本
   */
  version,
  /**
   * 是否展示错误信息
   */
  showErrorTips,
  /**
   * 请求超时时间
   */
  requestTimeout,
  /**
   * 默认深色皮肤
   */
  defaultTheme,
  /**
   * 后端服务器类型
   */
  backServerType,
  /**
   * 内部模块列表
   */
  moduleNames: moduleNames.split(','),
}
